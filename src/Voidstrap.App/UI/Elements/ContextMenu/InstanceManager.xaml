<base:WpfUiWindow x:Class="Voidstrap.UI.Elements.ContextMenu.InstanceManager"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:base="clr-namespace:Voidstrap.UI.Elements.Base"
                  xmlns:pages="clr-namespace:Voidstrap.UI.Elements.ContextMenu.InstancePages"
                  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                  Title="Instance Control"
                  Width="860" Height="580" MinWidth="760" MinHeight="480"
                  AllowsTransparency="False"
                  Background="{ui:ThemeResource ApplicationBackgroundBrush}"
                  ExtendsContentIntoTitleBar="True"
                  WindowBackdropType="None"
                  WindowStartupLocation="CenterScreen"
                  UseLayoutRounding="True"
                  SnapsToDevicePixels="True">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <ui:TitleBar x:Name="RootTitleBar" Grid.Row="0" Padding="8" Panel.ZIndex="1000" ForceShutdown="False" MinimizeToTray="False" UseSnapLayout="True" />
    <Grid x:Name="RootGrid" Grid.Row="1" Margin="12,8,0,0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <ui:NavigationStore x:Name="RootNavigation" Grid.Row="1" Grid.Column="0" Margin="0,0,12,0" Frame="{Binding ElementName=RootFrame}" SelectedPageIndex="0">
        <ui:NavigationStore.Items>
          <ui:NavigationItem Margin="0,0,0,8" Content="Instances" Icon="Games28" Tag="instances" PageType="{x:Type pages:InstancesPage}" />
          <ui:NavigationItem Margin="0,0,0,8" Content="Accounts" Icon="Person24" Tag="accounts" PageType="{x:Type pages:InstanceAccountsPage}" />
        </ui:NavigationStore.Items>
      </ui:NavigationStore>
      <Frame x:Name="RootFrame" Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" BorderThickness="0" NavigationUIVisibility="Hidden" />
    </Grid>
  </Grid>
</base:WpfUiWindow>
