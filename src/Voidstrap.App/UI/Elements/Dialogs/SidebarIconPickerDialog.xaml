<base:WpfUiWindow x:Class="Voidstrap.UI.Elements.Dialogs.SidebarIconPickerDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:base="clr-namespace:Voidstrap.UI.Elements.Base" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" Title="Choose sidebar icon" Width="540" Height="650" MinWidth="440" MinHeight="480" ExtendsContentIntoTitleBar="True" ResizeMode="CanResize" WindowStartupLocation="CenterOwner" Background="{ui:ThemeResource ApplicationBackgroundBrush}" Loaded="Window_Loaded">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ui:TitleBar x:Name="RootTitleBar" Grid.Row="0" Padding="8" Title="Choose sidebar icon" Icon="pack://application:,,,/Voidstrap.png" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" />
    <ui:TextBox x:Name="SearchBox" Grid.Row="1" Margin="18,12,18,10" Height="38" PlaceholderText="Search all WPF UI icons" TextChanged="SearchBox_TextChanged" />
    <ListBox x:Name="IconList" Grid.Row="2" Margin="18,0" BorderThickness="1" BorderBrush="{DynamicResource ControlElevationBorderBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
      <ListBox.ItemsPanel>
        <ItemsPanelTemplate>
          <VirtualizingStackPanel />
        </ItemsPanelTemplate>
      </ListBox.ItemsPanel>
      <ListBox.ItemContainerStyle>
        <Style TargetType="ListBoxItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
          <Setter Property="Padding" Value="10,7" />
        </Style>
      </ListBox.ItemContainerStyle>
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="34" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:SymbolIcon Grid.Column="0" Symbol="{Binding Icon}" FontSize="22" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding Name}" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
          </Grid>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <Border Grid.Row="3" Margin="0,12,0,0" Padding="18,14" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ControlElevationBorderBrush}" Background="{DynamicResource ControlFillColorDefaultBrush}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal">
          <ui:Button Height="34" Icon="Image24" Content="Custom image" Click="CustomImageButton_Click" />
          <ui:Button Height="34" Margin="8,0,0,0" Icon="ArrowReset24" Content="Default" Click="DefaultButton_Click" />
        </StackPanel>
        <StackPanel Grid.Column="2" Orientation="Horizontal">
          <ui:Button Height="34" MinWidth="86" Content="Cancel" Click="CancelButton_Click" />
          <ui:Button Height="34" MinWidth="96" Margin="8,0,0,0" Appearance="Primary" Content="Use icon" Click="UseIconButton_Click" />
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</base:WpfUiWindow>
