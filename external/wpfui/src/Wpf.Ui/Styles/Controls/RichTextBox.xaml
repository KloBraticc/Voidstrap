<!--
    This Source Code Form is subject to the terms of the MIT License.
    If a copy of the MIT was not distributed with this file, You can obtain one at https://opensource.org/licenses/MIT.
    Copyright (C) Leszek Pomianowski and WPF UI Contributors.
    All Rights Reserved.
-->

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Thickness x:Key="RichTextBoxAccentBorderThemeThickness">0,0,0,1</Thickness>
    <Thickness x:Key="RichTextBoxFocusBorderThemeThickness">0,0,0,2</Thickness>
    <Duration x:Key="RichTextBoxIndicatorDuration">0:0:0.25</Duration>
    <QuinticEase x:Key="RichTextBoxIndicatorEase" EasingMode="EaseOut" />

    <Storyboard x:Key="RichTextBoxFocusStoryboard">
        <DoubleAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource RichTextBoxIndicatorDuration}" EasingFunction="{StaticResource RichTextBoxIndicatorEase}" />
        <ThicknessAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="BorderThickness" To="{StaticResource RichTextBoxFocusBorderThemeThickness}" Duration="{StaticResource RichTextBoxIndicatorDuration}" EasingFunction="{StaticResource RichTextBoxIndicatorEase}" />
    </Storyboard>

    <Storyboard x:Key="RichTextBoxBlurStoryboard">
        <DoubleAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource RichTextBoxIndicatorDuration}" EasingFunction="{StaticResource RichTextBoxIndicatorEase}" />
        <ThicknessAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="BorderThickness" To="{StaticResource RichTextBoxAccentBorderThemeThickness}" Duration="{StaticResource RichTextBoxIndicatorDuration}" EasingFunction="{StaticResource RichTextBoxIndicatorEase}" />
    </Storyboard>

    <Style TargetType="{x:Type RichTextBox}">
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource RinPrimaryColorBrush}" />
        <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RinControlBorderBrush}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,5,10,7" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RichTextBox}">
                    <Grid>
                        <Border
                            x:Name="MainBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}" />
                        <Border
                            x:Name="AccentBorder"
                            BorderBrush="{DynamicResource RinTextControlBorderBrush}"
                            BorderThickness="{StaticResource RichTextBoxAccentBorderThemeThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            IsHitTestVisible="False" />
                        <Border
                            x:Name="FocusBorder"
                            BorderBrush="{DynamicResource RinPrimaryColorBrush}"
                            BorderThickness="{StaticResource RichTextBoxAccentBorderThemeThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            IsHitTestVisible="False"
                            Opacity="0" />
                        <ScrollViewer
                            x:Name="PART_ContentHost"
                            Margin="0,0,2,0"
                            Padding="{TemplateBinding Padding}"
                            VerticalAlignment="Top"
                            Foreground="{TemplateBinding Foreground}"
                            HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                            VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource ControlFillColorInputActiveBrush}" />
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource RichTextBoxFocusStoryboard}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource RichTextBoxBlurStoryboard}" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MainBorder" Property="Opacity" Value="0.4" />
                            <Setter TargetName="AccentBorder" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
