<!--
    This Source Code Form is subject to the terms of the MIT License.
    If a copy of the MIT was not distributed with this file, You can obtain one at https://opensource.org/licenses/MIT.
    Copyright (C) Leszek Pomianowski and WPF UI Contributors.
    All Rights Reserved.

    Based on Microsoft XAML for Win UI
    Copyright (c) Microsoft Corporation. All Rights Reserved.
-->

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <Thickness x:Key="NumberBoxBorderThemeThickness">1</Thickness>
    <Thickness x:Key="NumberBoxAccentBorderThemeThickness">0,0,0,1</Thickness>
    <Thickness x:Key="NumberBoxFocusBorderThemeThickness">0,0,0,2</Thickness>
    <Thickness x:Key="NumberBoxThemePadding">10,4,10,7</Thickness>
    <Thickness x:Key="NumberBoxLeftIconMargin">10,0,0,0</Thickness>
    <Thickness x:Key="NumberBoxRightIconMargin">0,0,10,0</Thickness>
    <Thickness x:Key="NumberBoxSpinButtonMargin">0,0,4,0</Thickness>
    <system:Double x:Key="NumberBoxButtonIconSize">14</system:Double>
    <Duration x:Key="NumberBoxIndicatorDuration">0:0:0.25</Duration>
    <QuinticEase x:Key="NumberBoxIndicatorEase" EasingMode="EaseOut" />

    <Storyboard x:Key="NumberBoxFocusStoryboard">
        <DoubleAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource NumberBoxIndicatorDuration}" EasingFunction="{StaticResource NumberBoxIndicatorEase}" />
        <ThicknessAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="BorderThickness" To="{StaticResource NumberBoxFocusBorderThemeThickness}" Duration="{StaticResource NumberBoxIndicatorDuration}" EasingFunction="{StaticResource NumberBoxIndicatorEase}" />
    </Storyboard>

    <Storyboard x:Key="NumberBoxBlurStoryboard">
        <DoubleAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource NumberBoxIndicatorDuration}" EasingFunction="{StaticResource NumberBoxIndicatorEase}" />
        <ThicknessAnimation Storyboard.TargetName="FocusBorder" Storyboard.TargetProperty="BorderThickness" To="{StaticResource NumberBoxAccentBorderThemeThickness}" Duration="{StaticResource NumberBoxIndicatorDuration}" EasingFunction="{StaticResource NumberBoxIndicatorEase}" />
    </Storyboard>

    <Style x:Key="DefaultUiNumberBoxStyle" TargetType="{x:Type controls:NumberBox}">
        <!--  Universal WPF UI focus  -->
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <!--  Universal WPF UI focus  -->
        <!--  Universal WPF UI ContextMenu  -->
        <Setter Property="ContextMenu" Value="{DynamicResource DefaultControlContextMenu}" />
        <!--  Universal WPF UI ContextMenu  -->
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="IconForeground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource RinPrimaryColorBrush}" />
        <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource RinControlBorderBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource NumberBoxBorderThemeThickness}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="MinHeight" Value="{DynamicResource TextControlThemeMinHeight}" />
        <Setter Property="MinWidth" Value="{DynamicResource TextControlThemeMinWidth}" />
        <Setter Property="Padding" Value="{StaticResource NumberBoxThemePadding}" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="ClearButtonEnabled" Value="True" />
        <Setter Property="SpinButtonsEnabled" Value="True" />
        <Setter Property="Icon" Value="Empty" />
        <Setter Property="IconFilled" Value="False" />
        <Setter Property="IconPlacement" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NumberBox}">
                    <Grid HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <Border
                            x:Name="ContentBorder"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}" />
                        <Border
                            x:Name="AccentBorder"
                            BorderBrush="{DynamicResource RinTextControlBorderBrush}"
                            BorderThickness="{StaticResource NumberBoxAccentBorderThemeThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            IsHitTestVisible="False" />
                        <Border
                            x:Name="FocusBorder"
                            BorderBrush="{DynamicResource RinPrimaryColorBrush}"
                            BorderThickness="{StaticResource NumberBoxAccentBorderThemeThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            IsHitTestVisible="False"
                            Opacity="0" />
                        <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <controls:SymbolIcon
                                x:Name="ControlIconLeft"
                                Grid.Column="0"
                                Margin="{StaticResource NumberBoxLeftIconMargin}"
                                Padding="0"
                                VerticalAlignment="Center"
                                Filled="{TemplateBinding IconFilled}"
                                Foreground="{TemplateBinding IconForeground}"
                                Symbol="{TemplateBinding Icon}" />
                            <Grid Grid.Column="1" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Decorator
                                    x:Name="PART_ContentHost"
                                    Margin="0"
                                    VerticalAlignment="Center"
                                    TextElement.Foreground="{TemplateBinding Foreground}" />
                                <TextBlock
                                    x:Name="PlaceholderTextBox"
                                    Margin="0"
                                    Padding="2,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    IsHitTestVisible="False"
                                    Text="{TemplateBinding PlaceholderText}"
                                    TextTrimming="CharacterEllipsis" />
                            </Grid>
                            <controls:Button
                                x:Name="ClearButton"
                                Grid.Column="2"
                                Command="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                CommandParameter="clear"
                                Style="{DynamicResource RinTextBoxClearButtonStyle}" />
                            <controls:Button
                                x:Name="IncrementButton"
                                Grid.Column="3"
                                Margin="{StaticResource NumberBoxSpinButtonMargin}"
                                Command="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                CommandParameter="increment"
                                FontSize="{StaticResource NumberBoxButtonIconSize}"
                                Icon="ChevronUp20"
                                Style="{DynamicResource RinTextBoxClearButtonStyle}" />
                            <controls:Button
                                x:Name="DecrementButton"
                                Grid.Column="4"
                                Margin="{StaticResource NumberBoxSpinButtonMargin}"
                                Command="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                CommandParameter="decrement"
                                FontSize="{StaticResource NumberBoxButtonIconSize}"
                                Icon="ChevronDown20"
                                Style="{DynamicResource RinTextBoxClearButtonStyle}" />
                            <controls:SymbolIcon
                                x:Name="ControlIconRight"
                                Grid.Column="5"
                                Margin="{StaticResource NumberBoxRightIconMargin}"
                                Padding="0"
                                VerticalAlignment="Center"
                                Filled="{TemplateBinding IconFilled}"
                                Foreground="{TemplateBinding IconForeground}"
                                Symbol="{TemplateBinding Icon}" />
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="PlaceholderEnabled" Value="False">
                            <Setter TargetName="PlaceholderTextBox" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowClearButton" Value="False">
                            <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ClearButton" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="ClearButtonEnabled" Value="False">
                            <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ClearButton" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="SpinButtonsEnabled" Value="False">
                            <Setter TargetName="IncrementButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="DecrementButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IconPlacement" Value="Left">
                            <Setter TargetName="ControlIconRight" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ControlIconRight" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="IconPlacement" Value="Right">
                            <Setter TargetName="ControlIconLeft" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ControlIconLeft" Property="Margin" Value="0" />
                        </Trigger>
                        <Trigger Property="Icon" Value="Empty">
                            <Setter TargetName="ControlIconRight" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ControlIconRight" Property="Margin" Value="0" />
                            <Setter TargetName="ControlIconLeft" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ControlIconLeft" Property="Margin" Value="0" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ControlFillColorInputActiveBrush}" />
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource NumberBoxFocusStoryboard}" />
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource NumberBoxBlurStoryboard}" />
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter Property="Cursor" Value="IBeam" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Opacity" Value="0.4" />
                            <Setter TargetName="AccentBorder" Property="Opacity" Value="0.4" />
                            <Setter Property="ClearButtonEnabled" Value="False" />
                            <Setter Property="SpinButtonsEnabled" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultUiNumberBoxStyle}" TargetType="{x:Type controls:NumberBox}" />

</ResourceDictionary>
